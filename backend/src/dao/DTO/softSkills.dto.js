class SoftSkillDTO {
    constructor(title, subTitle, percentage, userId) {
        this.title = title;
        this.subTitle = subTitle;
        this.percentage = percentage;
        this.userId = userId;
    }
}

export default SoftSkillDTO;